name: jogadores_da_copa_api
description: A server app using the shelf package and Docker.
version: 1.0.0
# homepage: https://www.example.com
environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  args: ^2.0.0
  dotenv: ^3.0.0
  functions_framework: ^0.4.1
  get_it: ^7.2.0
  injectable: ^1.5.0
  logger: ^1.1.0
  shelf: ^1.1.0
  shelf_router: ^1.0.0

dev_dependencies:
  build_runner: ^2.3.2
  http: ^0.13.0
  injectable_generator: ^1.5.2
  lints: ^2.0.0
  shelf_router_generator: ^1.0.4
  test: ^1.15.0
